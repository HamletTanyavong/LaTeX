% Python lstset Configuration

% See https://ctan.org/pkg/listings for more information.

\lstset{
  language = Python,
  basicstyle = \fontfamily{pcr}\selectfont,
  tabsize = 4,
  breaklines = true,
  showstringspaces = false,
  frame = l,
  numbers = left,
  keywordstyle = \color{blue},
  identifierstyle = \color{black},
  commentstyle = \color{gray},
  escapechar = ~,
  escapeinside = {@*}{*@}
}
